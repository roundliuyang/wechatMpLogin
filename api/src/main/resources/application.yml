spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/wechat_login?useUnicode=true&characterEncoding=utf-8&allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=Asia/Shanghai"
    username: "root"
    password: "123456"
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: "update"

server:
  # 配置端口
  port: ${port:8082}
  servlet:
    session:
      timeout: 60m

# 公众号配置， 换成自己的配置
wx:
  mp:
    appid: wx55e8a97cc94d66e5
    secret: wxpingtaikan
    token: yunzhi
    aesKey: 123